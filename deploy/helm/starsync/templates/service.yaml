apiVersion: v1
kind: Service
metadata:
  name: {{ include "starsync.fullname" . }}
  labels:
    {{- include "starsync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "starsync.selectorLabels" . | nindent 4 }}
